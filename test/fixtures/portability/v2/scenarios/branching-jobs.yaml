id: branching-jobs
name: branching jobs
start: webhook
steps:
  - id: webhook
    name: webhook
    enabled: true
    type: webhook
    next: fan-out
  - id: fan-out
    name: fan out
    adaptor: '@openfn/language-common@latest'
    expression: |
      fn(state => state)
    next:
      branch-a:
        condition: on_job_success
      branch-b:
        condition: on_job_failure
  - id: branch-a
    name: branch a
    adaptor: '@openfn/language-common@latest'
    expression: |
      fn(state => state)
  - id: branch-b
    name: branch b
    adaptor: '@openfn/language-common@latest'
    expression: |
      fn(state => state)
