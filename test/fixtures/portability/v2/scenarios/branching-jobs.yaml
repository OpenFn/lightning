id: branching-jobs
name: branching jobs
steps:
  - id: webhook
    name: webhook
    enabled: true
    type: webhook
    next: fan-out
  - id: fan-out
    name: fan out
    adaptors:
      - '@openfn/language-common@latest'
    expression: |
      fn(state => state)
    next:
      branch-a:
        condition: '!state.errors'
      branch-b:
        condition: '!!state.errors'
  - id: branch-a
    name: branch a
    adaptors:
      - '@openfn/language-common@latest'
    expression: |
      fn(state => state)
  - id: branch-b
    name: branch b
    adaptors:
      - '@openfn/language-common@latest'
    expression: |
      fn(state => state)
