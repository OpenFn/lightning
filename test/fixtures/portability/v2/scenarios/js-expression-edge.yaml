id: js-expression-edge
name: js expression edge
start: webhook
steps:
  - id: webhook
    name: webhook
    enabled: true
    type: webhook
    next: source-step
  - id: source-step
    name: source step
    adaptor: '@openfn/language-common@latest'
    expression: |
      fn(state => state)
    next:
      target-step:
        condition: |
          !!state.data && state.data.length > 0
        label: Only when payload present
  - id: target-step
    name: target step
    adaptor: '@openfn/language-common@latest'
    expression: |
      fn(state => state)
