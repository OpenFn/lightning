id: simple-webhook
name: simple webhook
start: webhook
steps:
  - id: webhook
    name: webhook
    enabled: true
    type: webhook
    next: greet
  - id: greet
    name: greet
    adaptor: '@openfn/language-common@latest'
    expression: |
      fn(state => state)
