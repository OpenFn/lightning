id: simple-webhook
name: simple webhook
steps:
  - id: webhook
    name: webhook
    enabled: true
    type: webhook
    next: greet
  - id: greet
    name: greet
    adaptors:
      - '@openfn/language-common@latest'
    expression: |
      fn(state => state)
