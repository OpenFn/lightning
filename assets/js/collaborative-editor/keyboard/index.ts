/**
 * Priority-based keyboard shortcut system
 *
 * @module keyboard
 */

export { KeyboardProvider, useKeyboardShortcut } from './KeyboardProvider';
export type { KeyboardHandlerOptions, KeyboardHandlerCallback } from './types';
